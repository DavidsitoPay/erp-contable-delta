# Los scripts de shell deben conservar LF sin importar el core.autocrlf de
# cada máquina — un CRLF en database/run_migrations.sh puede romperlo al
# correr en el runner de Ubuntu de GitHub Actions.
*.sh text eol=lf
